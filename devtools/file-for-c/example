#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
int main(void){
    char *argv[] = { "/bin/bash", "-c", "echo \"Hi\" > /tmp/example", NULL};
    char *envp[] = { NULL };
    execve(argv[0], argv, envp);
    printf("Hello, World!\n");
    return 0;
}